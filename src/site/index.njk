---
title: Welcome
name: homepage
---

{% extends 'layouts/base.njk' %}

{% block pageTitle %}{{ title }} - {{ super() }}{% endblock %}

{% block bodyId %}{{ name }}{% endblock %}

{% block content %}
  <section id="topline">
    <div class="wrapper">
      <h1>Redefining relationships between people, places and property</h1>
      <p>
        Mortar is a technology company building better services. Our products
        and solutions are driving the next generation of intelligent, accessible,
        user-led services.
      </p>
      <a class="cta light" href="#use-cases">Learn about our work</a>
    </div>
  </section>

<section id="features">
  <div class="wrapper">
    <div class="section-head">
      <h1>
        How <span class="mortar-purple">Mortar</span> can help to
        <span class="mortar-purple">transform your services</span>
      </h1>
      <p class="section-tagline"></p>
    </div>

    <div id="feature-list">
      <ul>
        {% for feature in features %}
        <li>
          <h3>{{ feature.title }}</h3>
          <p class="feature-tagline">{{ feature.tagline }}</p>
          {{ feature.subfeatures | markdown | safe }}
          
          <!-- Update the Learn More link to have dynamic text -->
          <a href="{{ feature.link }}" class="learn-more">{{ feature.learnMoreText }}</a>
        </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</section>

<section id="partners">
  <div class="partners-wrapper">
    <!-- Heading Above the Logos -->
    <h2 class="partners-heading">Reaching 1000s of users and businesses</h2>

    <!-- Ticker container -->
    <div class="partners-ticker">
      <ul class="partners-list">
        <!-- List of partner logos -->
        <!-- Your partner logos here -->
      </ul>
    </div>
  </div>
</section>

<section id="use-cases">
  <div class="use-cases-wrapper">
    <h2>Our Use Cases</h2>
    <p>Make every engagement count and maximize the impact of your service experience. Mortar brings together a global suite of tools to help deliver the solution you need.</p>

    <div class="use-cases-grid">
      {% for case in collections.useCases %}
      <div class="use-case-card">
        <!-- Title -->
        <h3>{{ case.title }}</h3>
        
        <!-- Strapline -->
        <p class="strapline">{{ case.strapline }}</p>
        
        <!-- Description -->
        <p>{{ case.description }}</p>

        <!-- Learn More Text Link -->
        <a href="{{ case.link }}" class="learn-more-link">Learn More</a>

        <!-- Inset Case Study Sentence Box at the bottom of the card -->
        <div class="case-study-sentence">
          <h4>Case Study</h4>
          <!-- Case study sentence with inline Read More link -->
          {{ case.case_study_sentence }}
          <a href="{{ case.read_more_link }}" class="read-more-link">Read more</a>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section id="insights">
  <div class="wrapper">
    <div class="section-head">
      <h2>Insights</h2>
      <p class="section-tagline">
        Mortar makes the application of intelligence, data, and user-led design
        easier and more affordable; to inspire services, experiences, and
        relationships to be more accessible and inclusive. Read our insights to
        find out how we are applying technology to build better services.
      </p>
    </div>

    <ul id="insight-list">
      {% for post in collections.insights %}
      <li class="{% if loop.index > 6 %}hidden{% endif %}">
        <a
          class="insight-title"
          href="{{ post.url }}"
          aria-label="Insight: {{ post.data.title }}"
        >
          <div
            class="insight-image"
            style="background-image: url('{{ '/static/images/insights/' + post.data.image | url }}')"
          >
            <div class="hover-overlay"></div>
          </div>
          <div class="insight-details">
            <time datetime="{{ post.data.date | w3date }}">{{ post.data.date | date }}</time>
            {{ post.data.title }}
          </div>
        </a>
      </li>
      {% endfor %}
    </ul>

    <button id="load-more-insights" class="cta">Load More Insights</button>
    <div id="insights-preloader" class="hidden">
      <div class="spinner"></div>
    </div>
  </div>
</section>
{% endblock %}

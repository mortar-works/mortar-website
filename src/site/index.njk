---
title: Welcome
name: homepage
---

{% extends 'layouts/base.njk' %}

{% block pageTitle %}{{ title }} - {{ super() }}{% endblock %}

{% block bodyId %}{{ name }}{% endblock %}

{% block content %}
  <section id="topline">
    <div class="wrapper">
      <h1>Redefining relationships between people and property</h1>

      <p>
        Mortar is an AI engine transforming traditional processes with real-time data analysis, automation, intelligent decision making, and financial integrity.
      </p>

      <a class="cta light" href="#use-cases">Learn about our use cases</a>
    </div>
  </section>

  <section id="features">
    <div class="wrapper">
      <div class="section-head">
        <h2>Features</h2>
        <p class="section-tagline">Start making AI assisted decisions</p>
      </div>

      <div id="feature-list">
        <ul>
        {% for feature in features  %}
          <li>
            <h3>{{ feature.title }}</h3>
            <p class="feature-tagline">{{ feature.tagline }}</p>
            {{ feature.subfeatures | markdown | safe }}
          </li>
        {% endfor %}
        </ul>
      </div>
    </div>
  </section>

  <section id="partners">
    <div class="wrapper">
      <h2>Our partners</h2>
      <ul>
        <li><img src="{{ '/static/images/partners/pwc-logo.svg' | url }}" alt="PwC" /></li>
        <li><img src="{{ '/static/images/partners/london-and-partners-logo.png' | url }}" alt="London & Partners" /></li>
        {# <li><img src="{{ '/static/images/partners/learning-and-work-institue.png' | url }}" alt="Learning and Work Institute" /></li>
        <li><img src="{{ '/static/images/partners/optivo-logo.png' | url }}" alt="Optivo" /></li> #}
        <li><img src="{{ '/static/images/partners/finance-innovation-lab-logo.jpg' | url }}" alt="Finance Innovation Lab " /></li>
        {# <li><img src="{{ '/static/images/partners/one-manchester-logo.png' | url }}" alt="One Manchester" /></li> #}
        {# <li><img src="{{ '/static/images/partners/metropolitan-logo.png' | url }}" alt="Metropolitan Housing Trust" />/li> #}
        <li><img src="{{ '/static/images/partners/the-young-foundation-logo.png' | url }}" alt="Young Foundation" /></li>
      </ul>
    </div>
  </section>

  <section id="use-cases">
    <div class="wrapper">
      <div class="section-head">
        <h2>Use cases</h2>
        <div class="section-tagline">
          <p>Find out how companies are working with Mortar to enhance workplace efficiency, engage consumers, and drive revenue. </p>
          <p></p>
        </div>
      </div>

      <ol>
      {% for useCase in collections.useCases %}
      {{ useCase.data.order | log }}
        <li>
          <h3>{{ useCase.data.title }}</h3>

          {% if useCase.data.homepageImage %}
            <figure>
              <img src="{{ '/static/images/homepage/' + useCase.data.homepageImage | url }}" alt="" />
            </figure>
          {% endif %}

          <div>
            {{ useCase.data.description | markdown | safe }}

            <a href="{{ useCase.url }}" class="cta">Learn more<span class="visually-hidden"> about {{ useCase.data.title }}<span></a>
          </div>
        </li>
      {% endfor %}
      </ol>
    </div>
  </section>

  <section id="insights">
    <div class="wrapper">
      <div class="section-head">
        <h2>Insights</h2>
        <p class="section-tagline">Learn more about how Mortar is revolutionising finance and transforming relationships between people and property.</p>
      </div>

      <ul>
      {% for post in collections.insights %}
        <li>
          <a class="insight-title" href="{{ post.url }}" aria-label="Insight: {{ post.data.title }}">
            <div class="insight-image" style="background-image: url('{{ '/static/images/insights/' + post.data.image | url }}')"></div>
            <div class="insight-details">
              <time datetime="{{ post.data.date | w3date }}">{{ post.data.date | date }}</time>
              {{ post.data.title }}
            </div>
          </a>
        </li>
      {% endfor %}
      </ul>
    </div>
  </section>
{% endblock %}
